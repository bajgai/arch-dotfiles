# Omarchy user keybinding overrides. Sourced AFTER Omarchy's defaults, so
# anything here wins. Omarchy's own files live in
# ~/.local/share/omarchy/default/hypr/ and are NOT managed by this repo.
#
# PLACEHOLDER — replace with your real overrides.
# Example:
# bindd = SUPER, T, Terminal, exec, $terminal
